import React from 'react';

const Portfolio = (): React.JSX.Element => {
    return (
        <main>
            <h2>Portfolio</h2>
            <p>portfolio</p>
        </main>
    );
};

export default Portfolio;
