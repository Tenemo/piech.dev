{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run tests",
            "type": "shell",
            "command": "npm test --silent",
            "problemMatcher": ["$tsc", "$eslint-stylish"]
        },
        {
            "label": "Run tests",
            "type": "shell",
            "command": "npm test --silent",
            "problemMatcher": ["$tsc", "$eslint-stylish"]
        },
        {
            "label": "Typecheck",
            "type": "shell",
            "command": "npm run tsc --silent"
        }
    ]
}
